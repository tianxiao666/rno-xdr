package com.hgicreate.rno.controllers;

/**
 * Created by hadoop on 17-2-8.
 */
//@RestController
//@RequestMapping("/tiles")
public class TilesController {
//    @Autowired
//    @Qualifier("tilesTemplate")
//    private JdbcTemplate tilesTemplate;

//    @RequestMapping("/{zoom}/{column}/{row}")
//    public byte[] getTiles(@PathVariable int zoom, @PathVariable int column, @PathVariable int row) {
////        Map<String, Object> map;
//        String query = " select tile_data from tiles " +
//                "where zoom_level=" + zoom +
//                " and tile_column=" + column +
//                " and tile_row= " + row;
//        List<Map<String, Object>> result= tilesTemplate.queryForList(query);
////        map = tilesTemplate.queryForMap(query);
//        if (result.size()>0 && result.get(0).size() > 0)
//            return (byte[]) result.get(0).get("tile_data");
//        else
//            return null;
//    }
}
