export class Area {
    constructor(
        public id?: number,
        public name?: string,
        public areaLevel?: number,
        public parentId?: number,
        public longitude?: number,
        public latitude?: number,
    ) {
    }
}
